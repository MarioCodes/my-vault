package es.msanchez.kotlin.skeleton.playzone.basic.idioms

class PersonConsumer {

    fun consume(person: Person) {
        val email: String = person.email
        var age: String = person.name
    }

}